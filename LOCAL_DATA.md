# Local data

The byte-preserved 2026-07-28 offload from the former composite workspace is at
`/Users/kian/Backups/semiconductors-local-data-2026-07-28`.

This repository's archived subtree is `semiconductor_atlas/` (418 files,
7,756,131,898 bytes). The archive also retains the shared `sources/` input and
the sibling Supply Intelligence subtree, so keep the archive whole. The current
Git checkout remains the canonical source tree; use the archive to recover
missing local payloads rather than replacing tracked files.

The complete archive contains 1,137 regular files totaling 7,879,688,624 bytes.
Its checksum manifest is
`/Users/kian/Backups/semiconductors-local-data-2026-07-28.SHA256SUMS`, with
SHA-256 `40ffb7efa95ee9d506011bf735c0db15227197e99128650595a3ad8aba2a50a5`.

Verify every archived file from the archive root:

```sh
shasum -a 256 -c ../semiconductors-local-data-2026-07-28.SHA256SUMS
```
